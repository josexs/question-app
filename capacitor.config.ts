import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'questions.frontend',
  appName: 'questions-frontend',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
